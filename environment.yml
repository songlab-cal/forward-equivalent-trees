name: fe-sim  # Replace with the desired name for your environment
channels:
  - defaults
  - conda-forge

dependencies:
  - python>=3.11
  - numpy
  - pandas
  - seaborn
  - jupyter
  - ipykernel  # Optional, but useful for managing kernels in Jupyter
  - pip
  - pip:
    - bdms-sim
  
